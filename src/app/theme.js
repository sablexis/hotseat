import { createTheme } from '@mui/material/styles';

 const theme = createTheme ({
  palette: {
    mode: 'light',
    primary: {
      main: '#bb2525',
    },
    secondary: {
      main: '#f29b9b',
    },
  },
});

export default theme;